<form method="post" action="{{ path('app_bilan_financier_delete', {'id': bilan_financier.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bilan_financier.id) }}">
    <button class="btn">Delete</button>
</form>
