{{ form_start(form) }}
 {{ form_row(form.depenses) }}
 {{ form_end(form) }}
{{ form_start(form) }}
    <div>
    {{form_label(form.depenses, 'Dépenses:') }}
    {{form_widget(form.depenses) }}
    {{form_errors(form.depenses) }}
    <button type="submit" class="btn btn-primary">Enregistrer</button><br><br>
    </div>
{{ form_end(form) }}