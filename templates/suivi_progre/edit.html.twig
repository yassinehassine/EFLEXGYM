{% extends 'index.html.twig' %}

{% block title %}Edit SuiviProgre{% endblock %}

{% block body %}
<style>
    .back-link {
        color: orange;
        /* Change color to your preferred color */
        text-decoration: none;
        /* Remove default underline */
        font-size: 24px;
        /* Adjust font size as needed */
        margin-right: 10px;
        /* Add some margin to the right */
    }

    .back-link:hover {
        color: orange;
    }
</style>
<div class="container">
    <a href="{{ path('app_suivi_progre_show', {'id': suivi_progre.id}) }}" class="back-link">
        <i class="fa-solid fa-arrow-left"></i>
    </a>
    <div class="d-flex align-items-center">
        <h1>Modifier Mes Mesures</h1>
    </div>
    {{ include('suivi_progre/_form.html.twig', {'button_label': 'Update'}) }}
    <br>
    <div class="text-center">
        {{ include('suivi_progre/_delete_form.html.twig') }}</div>
</div>
<br>
{% endblock %}