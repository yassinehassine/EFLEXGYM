{% extends 'back.html.twig' %}

{% block title %}Liste des Abonnements{% endblock %}

{% block body %}
    <h1>Liste des Abonnements</h1>
     <div>
        <!-- Formulaire pour le filtre par type -->
        <form method="get" action="{{ path('app_abonnement_index') }}">
            <label for="type_filter">Filtrer par type :</label>
            <select name="type_filter" id="type_filter">
                <option value="">Tous</option>
                <option value="annuel">Annuel</option>
                <option value="mensuel">Mensuel</option>
            </select>
            <button type="submit">Filtrer</button>
             <a href="{{ path('app_abonnement_index') }}" class="btn btn-primary">Actualiser</a>
        </form>
    </div>

    <div>
        <!-- Formulaire pour la recherche par nom -->
        <form method="get" action="{{ path('app_abonnement_index') }}">
    <label for="nom_adherent">Rechercher par nom :</label>
    <input type="text" id="nom_adherent" name="nom_adherent">
    <button type="submit">Rechercher</button>
</form>

    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Prix</th>
                <th>DateDebut</th>
                <th>DateFin</th>
                <th>Etat</th>
                <th>Adhérent</th>

              <th>Bilan Financier</th>

                <th>actions</th>
            </tr>
        </thead>
        <tbody>
       {% for abonnement in abonnements %}
    <tr>
        <td>{{ abonnement.id }}</td>
        <td>{{ abonnement.type }}</td>
        <td>{{ abonnement.prix }}</td>
        <td>{{ abonnement.dateDebut ? abonnement.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
        <td>{{ abonnement.dateFin ? abonnement.dateFin|date('Y-m-d H:i:s') : '' }}</td>
        <td>{{ abonnement.etat }}</td>
        <td>{{ abonnement.idAdherent.nom }}</td> 

        <td>{{ abonnement.idBilanFinancier.id }}</td> 
        
        <td>
           <td> <a href="{{ path('app_abonnement_show', {'id': abonnement.id}) }}"><i class="fas fa-eye"></i></a></td>
          
           <td>  {{ include('abonnement/_delete_form.html.twig', {'id': abonnement.id}) }}</td>
         <td>    <form method="post" action="{{ path('send_email') }}">
        <input type="hidden" name="abonnement_id" value="{{ abonnement.id }}">
         <button type="submit" class="btn btn-light"><i class="fas fa-envelope"></i></button>
                    </form> </td>
        </td>
    </tr>
{% else %}
    <tr>
        <td colspan="7">no records found</td>
    </tr>
{% endfor %}

        </tbody>
    </table>
   <div class="pagination" style="display: flex; justify-content: center; margin-top: 20px;">
        <a href="{{ path('app_abonnement_index', {'page': 1}) }}"><i class="fas fa-angle-double-left"></i></a>
        {% for i in 1..total_pages %}
            <a href="{{ path('app_abonnement_index', {'page': i}) }}" class="{{ current_page == i ? 'active' : '' }}">{{ i }}</a>
        {% endfor %}
        <a href="{{ path('app_abonnement_index', {'page': total_pages}) }}"><i class="fas fa-angle-double-right"></i></a>
    </div>

    <a href="{{ path('app_abonnement_new') }}" class="btn btn-success"><i class="fas fa-plus"></i> Ajouter un abonnement</a>
    <style>
        .pagination a {
            margin: 0 5px;
            padding: 5px 10px;
            border: 1px solid #ddd;
            color: #333;
            text-decoration: none;
        }

        .pagination a.active {
            background-color: #007bff;
            color: white;
        }

        .pagination a:hover {
            background-color: #ddd;
        }
    </style>
{% endblock %}

