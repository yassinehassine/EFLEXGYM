<form method="post" action="{{ path('app_suivi_progre_delete', {'id': suivi_progre.id}) }}"
    onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_progre.id) }}">
    <button class="btn btn-danger"><i class="fa-solid fa-trash"></i></button>
</form>